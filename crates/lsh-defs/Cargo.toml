[package]
name = "lsh-defs"
version = "0.19.0"  # source: /VERSION (synced by `make sync-version`; do not edit by hand)
edition.workspace = true
license.workspace = true
rust-version.workspace = true

build = "build/main.rs"

[features]
# Forwards to stdext, where the check macros live.
sanity = [
    "stdext/sanity",
    #
]

[dependencies]
lsh.workspace = true
stdext.workspace = true

[build-dependencies]
lsh.workspace = true
stdext.workspace = true
